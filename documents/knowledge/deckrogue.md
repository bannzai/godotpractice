# deckrogue 開発知見

## 戦闘と探索の設計

- 独自の世界観「燈火の巡礼」。ゲーム状態は autoload の Run に集約し、画面は状態を参照して描画する。カードの表示と効果解決は同じカタログを参照する。
- 階層内の選択肢だけをシャッフルすることで、ランダム性を持たせながら休憩・成長・最上階への到達可能性を保った。
- 使用中のカードは、効果を解決してから捨て札または廃棄へ移す。先に捨て札へ置くと、山札が空のドロー効果で使用中のカードを引き直す可能性がある。
- パワーは廃棄へ移して再使用を防ぎ、強化値は戦闘中の状態として持つ。次の戦闘開始時に戦闘状態を初期化し、永続するデッキと遺物から開始効果を適用する。
- 弱体は攻撃側、脆弱は被攻撃側の行動境界で減衰させる。敵が新しく付けた脆弱を付与直後に減らさず、意図には状態異常を含む実ダメージを表示する。

## 素材の準備

- オリジナルのSVGと、Python標準ライブラリで合成したWAVを使用した。`scripts/dev/generate_assets.py` は固定seedで画像・音声を同じ内容に再生成する。19点のSHA256一致、SVGのXML構文、PCMの読込み、クレジット記録漏れを確認した。
- 日本語は同梱したZen Old Minchoをテーマに指定した。OFL全文とCREDITSを各エクスポートのinclude_filterへ含め、OSのフォントに依存しない構成にした。
- SVGは元サイズ以上に大きく見せるとインポート時のラスタライズ解像度の影響を受ける。キャラクターは表示サイズ以上の原寸で作ると扱いやすい。

## Godotの検証で確認したこと

- `--headless --quit` で音楽を再生すると、即終了によりAudioStreamPlaybackWAVが残る場合があった。headlessでは音声出力自体が不要なので再生しない。
- 描画付きの撮影はミュート設定で再生自体を止め、起動録画は`movie.gd`で終了10フレーム前に音源を停止する。`_exit_tree`で停止するだけでは遅く、終了前に音声サーバーが解放を処理する時間が必要だった。警告行を除外する変更は行っていない。
- 音声再生中の終了時リークはGodot側にも既報がある。今回の対処はローカルとCIのログで確認した。
  https://github.com/godotengine/godot/issues/76745
- InputMapはイベントの存在確認だけでなく、`KEY_LEFT`等の列挙値から物理キーイベントを作り、`event_is_action`で対応と競合を検査した。整数コードの左右キー取り違えを検出できる。
- 手札を枚数に応じて縮めると、追加ドロー時に日本語の名前や効果がはみ出す。カード幅を保ち、`ScrollContainer.follow_focus`でキーボード・ゲームパッドでも横スクロールさせた。デッキ一覧もフォーカス追従を有効にした。
- カードの通常状態だけ紙色にすると、ホバー時に共通ボタンの暗い背景へ戻り、暗い文字が読めなくなる。カードのホバー・押下背景も紙色で定義した。
- ダメージ数字には輪郭と描画順を与え、敵の被弾色の上でも読めるようにした。手札の捨て演出はスクロール領域外の表示用カードを動かすことで、途中で切り取られないようにした。

## 検証の記録

- `make test GAMES=deckrogue`成功。カード定義・全効果・山全体の保存則・廃棄・致死攻撃後の効果解決・状態異常・報酬・遺物・休憩・入力イベントを検査した。
- 固定seedの通常操作だけで8階層からボス勝利、無抵抗で敗北、結果からタイトル・再開のループを検証した。複数seedで状態・選択・報酬・最終結果の再現性も確認した。
- `make screenshot GAMES=deckrogue`成功。タイトル、地図、戦闘、ドロー・プレイ・被弾・捨て、デッキ、報酬、イベント、休憩、ボス、勝敗、9枚の手札・ホバー・スクロール、弱体と脆弱の同時表示を撮影した。
- 撮影スクリプトはキーボードとゲームパッドの入力イベントを送り、開始・ノード選択・ターン終了・デッキ／地図の開閉も確認する。大量手札と状態異常の同時表示だけは表示専用の状態を一時投入して元に戻す。
- `make movie GAMES=deckrogue`成功。5秒の音声付きmp4を生成し、冒頭・中間・末尾を目視確認した。音声トラックが無音でないこともffmpegで確認した。
- `make build-all GAMES=deckrogue`と`make build-web GAMES=deckrogue`成功。macOS・Windows・Linux・Webの成果物を確認した。
- 初回の実装コミットに対するCIは全ジョブ成功。最終コミットのCI・映像artifact確認結果はPRに記録する。

## 共有物・skillへの改善提案

- godot-developmentへ、音声再生を伴う録画の終了前停止と、InputMapの実イベント対応検査を再利用できる例として追加するとよい。
- game-asset-searchへ、固定seedのSVG・音声合成と再生成一致の検証を補助的な素材準備経路として追加するとよい。
- 現在のCIは`documents/knowledge/deckrogue.md`も共有物変更として扱い、ゲーム単独のPRでも9ゲームすべてを検証する。知見文書の変更だけなら対応ゲームへ絞る改善が可能。共有スクリプトは変更していない。

## 残した判断点

なし。Steam連携・ストア公開・PRマージはプロジェクトの対象外。物理ゲームパッド実機は使用せず、同じInputMapを通る入力イベントで検証した。

## 第 2 ラウンド (品質向上)

### 制作方針

- 深い紺と青緑、羊皮紙色、燈火の金、朱色の布で配色を統一し、切り絵と細密な線画を組み合わせる。キャラクターごとの輪郭・装備・表情を独立した素材で持たせる。
- 戦闘ルールとカードカタログは維持し、表示・演出と検証を強化する。キャラクターの待機・移動・攻撃・被弾・死亡は独立したスプライトシートで表現する。
- アニメーションの開始・途中・終了、演出、操作付き録画、Web 実操作、終了時の音声解放までを証拠として残す。検証結果と制作中の知見は実施後に追記する。

### 素材とアニメーションで得た知見

- 7種のキャラクターを各1枚の独立したシートにし、待機・移動・攻撃・被弾・死亡を各6コマで制作した。腕・翼・装備・冠などの部位を動かすと、全体の移動だけより動作の意味が伝わる。カタログの「岩角の獣」に従来は霊炎の絵が割り当てられていたため、岩角の四足獣を別に制作した。
- 入れ子の `<svg x="..." y="...">` はlibrsvgでは正しく描画されたが、Godotでは位置が無視され、全30コマが左上へ重なった。`<g transform="translate(...)">` に変更して解消した。外部描画器だけで素材を合格にせず、Godotがインポートした各コマの非透明領域とポーズの違いをselfcheckで検査する。
- 攻撃・死亡では腕や武器がコマを越えやすい。全210コマを余白付きに描画し、アルファ値のある範囲が256×320内に収まることを確認した。SVG56点とWAV13点はそれぞれ固定seedによる再生成でSHA256が一致した。
- `AnimatedSprite2D` と `AtlasTexture` でキャラごとの素材と共通の再生制御を分離できる。撮影では再生を止めてフレームを固定し、開始・途中・終了を同じ条件で比較する。実際の動きは実入力の録画で別に確認する。
- 被弾のヒットストップはキャラの `speed_scale` だけを65ms止め、UIと音楽の時間は止めない。画面再描画が早すぎると被弾最終コマや数字のフェードが切れるため、被弾完了を待ち、同じ戦闘中の一時演出は手札再描画から独立して保持する。
- 背景の視差と霧も手札再描画から独立させる。状態が変わるたびに背景を作り直すと、視差や霧の進行が毎回先頭へ戻る。
- `ProgressBar` はパーセント表示を消す前にサイズを設定すると、既定の最小高さが残って文字を覆った。スタイルと表示設定を確定した後にサイズを設定し直して解消した。
- 手続きSVGは透過・配色・部位の統一・再生成に向いている。今回は切り絵と線画の方向を採用した。絵画的な質感や複雑な身体運動をさらに求める場合は、専任の作画工程との比較が必要になる。

### 音と終了処理

- BGMをタイトル・探索・戦闘・ボス・敗北・勝利の6曲へ分け、撥弦・倍音のある笛・持続音・低音・金管・ベル・打楽器を重ねた。SEはカード・攻撃・防御・回復・強化・死亡・遷移の7種。全素材は独自合成で、PCM形式・非無音・非クリッピング・両端ゼロを検査した。
- Godot 4.7 は `--quit` と `--quit-after` を消費し、`OS.get_cmdline_args()` に残さない。引数を読んで終了フレームを先取りする案では強制終了に対応できなかった。
- 通常終了は `Sound.shutdown()` で音源を停止し、出力遅延の2倍以上かつ120ms以上と数フレームを待つ。`--quit-after` では終了通知後にawaitで延命できないため、`_exit_tree()`で停止後に実時間を待つ。この経路だけ同期する理由をコードに記録した。
- 音源を実再生する専用SceneTreeで、通常shutdown・`--quit`・`--quit-after 1/30/120` を実行し、CoreAudioとDummyの各条件でexit 0、ログ全文にWARNING / ERRORなしを確認した。headlessの再生抑止だけで検証を代替していない。

### 検証と道具の改善案

- `make -C games/deckrogue movie-play` は30fps・30秒の実入力録画。通常の開始操作後に乱数種だけ固定し、カード2枚をクリックして使い、敵の攻撃で自然敗北し、結果画面からタイトルへ戻る。体力や敵HPを撮影のために変更しない。
- スクリーンショットは全キャラ5動作の開始・途中・終了15枚、6種の演出、視差の両端と既存の代表画面・大量手札・状態異常を撮る。撮影用PNGをGodotが再インポートしないよう、tmpを作る際に `.gdignore` も用意する。
- `godot-development` への再利用候補は、コマごとの透明領域検査、フレームを固定した連続撮影、実入力録画、音声の終了時解放検証。共有skillは変更せず、このゲームの生成・検証スクリプトを提案例として残す。
- 現在の共有CIはknowledgeの変更でも全ゲームを実行し、初回CIでは変更対象外のcrewrtsに音声リソースの終了時リークが出た。ゲーム別文書を変更対象のゲームへ対応付ける共有PR #23が改善経路。共有ファイルや他ゲームはこの作業から変更しない。

### 第 2 ラウンドの検証結果

- ローカルで `make test GAMES=deckrogue`、`make screenshot GAMES=deckrogue`、`make movie GAMES=deckrogue`、`make -C games/deckrogue movie-play`、`make build-all GAMES=deckrogue`、`make build-web GAMES=deckrogue` がすべてexit 0。ログ全文にWARNING / ERRORなし。macOS・Windows・Linux・Webの成果物を生成した。
- PR #25が未マージのため、指定された代替の `make -C games/deckrogue run` で起動した。今回のworktreeで起動したPIDを確認して通常のアプリ終了要求を送り、exit 0とリーク警告なしを確認。完成したメインシーンでも `--quit-after 1/30/120` がすべてexit 0、WARNING / ERRORなし。
- ローカル撮影42枚を目視し、7キャラ×5動作×開始・途中・終了、6種の演出、背景、各画面に欠けや文字の重なりがないことを確認した。30秒の実入力録画を2秒間隔の15枚と末尾画像で確認し、カード使用・敵行動・敗北・タイトル復帰まで到達した。
- 実装コミット `0f5ac32` のCIはrun `34025602623` で全ジョブ成功。deckrogueは初回から成功し、変更対象外のcrewrtsだけ終了時リークで失敗したため、そのジョブを2回再実行した。deckrogueのartifactのPNG42枚とmp4の2秒間隔・末尾フレームも目視した。LinuxのV-Sync非対応警告は既存Makefileの許容行で、音声リークや描画エラーはない。
- webtunnelを `--software-webgl --ref polish/deckrogue` で起動し、runnerのChromiumでEnterによる開始・地図選択・カード使用、Eによるターン終了、敗北結果からマウスクリックによるタイトル復帰を確認した。ゲーム状態や体力は変更していない。6枚のスクリーンショットを撮影し、ブラウザーの実行エラーは0件。検証後に `down deckrogue` を実行した。
- webtunnelではGodotの描画はcanvas内なので、DOMの要素一覧では手札や体力を読めない。起動はstatus要素の消滅で確認し、操作後の状態は画像で確認する。キーボード操作なら座標変換が不要で、マウス操作は実際のcanvas領域と中央寄せの余白から座標を求める。SwiftShaderでは静止画を撮るまでに数秒かかるため、実時間の滑らかさの判断はローカルの固定fps録画で補う。

### 本番に向けた判断点

- 切り絵と細密線画の方向を採用し、待機は小さく、攻撃・被弾・死亡を大きく動かした。美術の好みと、動きの強さ・音楽の密度はPRの画像・プレイ録画でレビューできるようにした。
- Steam連携・公開・マージは今回の対象外。物理ゲームパッドを使った実機検証は第1ラウンド同様に行っておらず、InputMapを通る入力イベントの検証を維持している。Windows・Linuxはエクスポートを確認し、Linuxの描画はCIで確認した。

## 手直しラウンド (見た目の特色化)

### 分かりにくさの原因と効いた表現

- 以前は上端のHUD、下端の共通操作ガイド、暗い共通パネルという構成が他ゲームと同じだった。地図の各ノードも同じ大きさの箱が横一列に並び、選べる場所、選んだ先で起こること、次に進行させる操作の視覚的な優先順位が弱かった。
- 画面全体を木の机に置いた羊皮紙の見開きに変え、タイトル、地図、戦闘、カード帳、結果をすべて「同じ写本の別の頁」として構成した。上端の情報も本文と同じ紙面へ収め、画面下の共通操作ガイド行は廃止した。
- 地図は城・川・森の銅版画を薄く敷き、選択済みの道を朱、次に選べる枝を金の線で描く。選択可能な札だけを金縁にし、フォーカスとホバーで右上の予告を更新することで、決定前に戦闘・強敵・休息・書庫・祠の違いが分かるようになった。
- 戦闘では「敵が次に書く一手」を右頁上部へ独立させ、中央に使える墨、下部に金縁の手札と「この葉を閉じる」を置いた。墨が足りないカードは灰色にし、「墨が何点足りないか」をカード上へ直接表示する。ツールチップだけに理由を置かないことで、キーボード・ゲームパッドでも同じ情報が見える。
- 初回だけ、地図の読み方、選択操作、敵の意図、カードとターン終了を4つの欄外注で順に説明する。各注に読み飛ばすボタンを置き、2回目以降の旅では出さない。常設の操作一覧より場面と対象を結び付けやすく、既存プレイヤーの操作を妨げない。

### Public Domain 素材の収集と加工

- `game-asset-search` の手順で、まず所蔵館またはWikimedia Commonsの作品ページにある作者、制作年、権利表示、原画像URLを確認した。候補の小さいプレビューを並べ、城・道・森、騎士・怪物、彩色飾り文字という不足役割を埋められる6点へ絞った。取得した原画像は `tmp` に置き、ビルドとリポジトリには加工済みPNGだけを含める。
- 当たりだったのは、Rijksmuseum由来の城と川のCC0銅版画、National Gallery of Art由来の騎士と竜のCC0木版画、The Metropolitan Museum of Art由来のCC0彩色飾り文字。大きな画像のため、タイトル、地図、カード、人物の別々のクロップにも線が残った。Doréの森とAberdeen BestiaryはPublic Domainの根拠が明確で、戦闘背景とボス頁に異なる密度を足せた。
- British Library Flickr Commonsの古書挿絵は公開年と関係作者の没年まで確認し、1844年刊でPublic Domain、Flickr Commons上の表示は「No known copyright restrictions」と記録した。この表示はCC0の許諾文ではないため、制作年が新しい候補では根拠として扱わない。文字・書名・紋章が大きい表紙は、固有名称やロゴを持ち込むため不採用にした。
- `process_pd_art.py` は正規化座標でクロップし、Pillowで縮小、オートコントラスト、羊皮紙色へのデュオトーン、細い額縁を決定的に適用する。彩色写本だけは色を残す。元画像の白背景を透過して切り抜くと輪郭に白い縁が出やすかったため、古書の矩形パネルとして見せる方が加工量も少なく、版画の紙質も残った。
- 7人物は同じ画像を使い回さず、騎士、彩色の獣、塔、竜、森の人物など異なるクロップを基にした。各人物を一枚の版画札として、待機・移動・攻撃・被弾・死亡の5行、各6コマへ配置した。部位アニメーションに比べると動きは札全体の移動・回転が中心だが、前ラウンドの7人物×5動作×6コマと戦闘演出の契約は維持できる。
- 検証は加工後の一覧画像を先に目視し、その後GodotがインポートしたPNGを撮影する二段階にした。selfcheckでは7枚の1536×1600シート、各256×320コマの非空領域、各動作で3種類以上の異なる画像、`assets/art`にSVGが残っていないことを確認する。CREDITSの全素材名もファイル走査で照合する。
- 手続きSVGは小容量で配色・輪郭・部位を直しやすく、入力なしで完全再生成できる。一方、複数ゲームで同じ生成方法を使うと形・余白・色が似やすく、古書の線密度や偶然性は出にくかった。Public Domain画像は見ただけで時代と媒体が伝わる反面、作品ごとの権利確認、原画の取得、使える部分の探索、色と線の揃え方に時間がかかる。今回は「矩形の版画札」という共通加工に限定することで、異なる原画を一冊のUIへまとめた。

### Godotで写本UIを作る時の注意

- `TextureRect`で紙の全面画像を強く出すと本文のコントラストが落ちる。紙地は低い不透明度で左右頁へ別々に重ね、文字、選択枠、進行線はその上へ不透明で描く。外部画像を背景にしても、操作対象の境界色は画像内の色へ依存させない。
- `Line2D`へ1点だけ渡しても線にはならない。旅の開始直後は完了経路が1点なので、2点以上ある時だけpointsを設定し、次の候補へ伸びる金線は2点ずつ別に作る。
- 画面を`queue_free()`して同じフレームで次画面を作り、直後にスクリーンショットを撮ると、旧画面とフェード中の新画面が一枚に重なる。遷移の正常な途中表現と区別するため、代表画面は0.32秒の遷移完了後に撮影し、演出途中は別の名前で意図的に撮る。
- チュートリアルはゲーム状態を進めず、UI側のセッション内フラグと段階だけを持つ。地図の案内を閉じた段階を保持したまま実際の地図操作を許し、戦闘へ遷移した時に残りの注を出すと、説明と対象を同じ画面に置ける。
- カード帳のカードは見た目を戦闘手札と共有しても、`focus_mode`と`mouse_filter`を無効にして操作可能に見せない。戦闘中の選択不可カードは無効色に加え、理由を不透明な紙片の上へ置くと版画の線に文字が埋もれない。
- 場面別BGMは維持し、タイトル・地図・戦闘・ボス・結果ごとに風、焚火、紙の環境音を最大3層まで薄く重ねた。頁遷移と地図の決定に紙・羽根ペンの短い音を付けると、画像だけでなく音でも写本の操作だと伝わる。環境音を切り替える時は古い`AudioStreamPlayer`のstream参照も外し、終了時リークを防ぐ。

### skill化の候補

- `game-asset-search` に、所蔵館のOpen Access表示、WikimediaのPublic Domainテンプレート、Flickr Commonsの「No known copyright restrictions」を同一視せず記録する確認表と、原画から派生した全ファイルをCREDITSへ対応付けるひな形を追加すると再利用しやすい。
- `game-asset-search` に、プレビュー一覧、正規化クロップ、デュオトーン、枠付け、加工済み一覧の生成までを行うPillowスクリプト例があると、原画をコミットせず派生PNGだけを管理する案件で手間を減らせる。
- `godot-development` に、画面差し替え後の代表撮影ではTween完了を待つこと、フォーカス・ホバー・無効理由・モーダル背面を別々に撮ること、`Line2D`の0点・1点境界をselfcheckへ入れることを追記する候補がある。共有skill自体はこのゲームの作業範囲外なので変更していない。
