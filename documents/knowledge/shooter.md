# shooter の実装知見

## 実装方針

- 150 秒の出現データと最後のボス戦で、約 3 分のステージを構成する。
- 状態と保存は autoload、敵の出現・弾の方向は純粋なロジック、描画と衝突処理はゲームシーンに分ける。
- 日本語フォントを同梱し、macOS のフォントフォールバックに依存しない。
- 代表画面に加えて入力イベントと実ゲームループの検証を行い、ローカルと CI の描画を確認する。

## 進捗

実装中。検証結果と判断の根拠は実測後に追記する。
